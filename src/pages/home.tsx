import React from 'react';

const Home: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Model Builder</h1>
            <p>Create and manage your JSON schemas with ease.</p>
        </div>
    );
};

export default Home;